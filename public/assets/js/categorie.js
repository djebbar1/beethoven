/*window.onload =()=>{
    const selectElt =document.querySelector(".dropdown-menu");
    const selectDiv =document.querySelector(".dropdown-item");*/
    
   /* const newSelect =document.createElement("div");
    newSelect.classList.add("new-select");
    newSelect.innerHTML= selectElt.option[selectElt.selectedIndex].innerHTML;
    selectDiv.appendChild(newmenu);*/

    /*newSelect.addEventListener("click", function(){
       let saisie = this.textContent.toLowerCase();
       for(let option of newMenu.children){
        if(option.textContent.toLowerCase().search(saisie) > -1){
            option.style.display ="block";
        }else{
            option.style.display ="none";
        }

       }

            
        

    });
}
import { useState } from "react";*/
/*window.onload =()=>{
function chang() {
  const [isClicked, setIsClicked] = useState(false);

  const handleChange = (event) => {
    setIsClicked(event.target.clicked);
  };
  return (
    <form>
      <label htmlFor="color">
      <a href="{{path('app_categories')}} "><li class="dropdown-item text-black mt-0 name" onclick="{isclicked}">{{categories}}</li> </a>
      </label>

      {isChecked && <div>Blue is selected!</div>}
    </form>
  );
}
console.log('');
}
export default clicked;*/